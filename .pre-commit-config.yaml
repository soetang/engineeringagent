repos:
  - repo: local
    hooks:
      - id: agent-harness-precommit
        name: agent-harness-precommit
        entry: uv run python scripts/gates.py run --profile precommit
        language: system
        pass_filenames: false
