repos:
  - repo: local
    hooks:
      - id: engineeringagent-precommit
        name: engineeringagent-precommit
        entry: uv run python scripts/gates.py run --profile precommit
        language: system
        pass_filenames: false
