id: FEAT-169
title: Remove stale legacy gates module references from fitness policy and tests
type: chore
expected_commit_subject: 'chore: remove stale legacy gates module references'
planning_tier: planned
status: done
priority: high
objective: Ensure active runtime policy and test suites no longer encode legacy behavior
  around the removed `engineeringagent.gates` module.
context: 'The production module path `src/engineeringagent/gates.py` has already been
  removed,

  but active fitness policy/test assets still reference it as if it were an allowlisted

  or special-case module.


  These stale references create policy drift, make fitness fixtures less representative

  of current architecture, and preserve migration-era behavior assumptions that should

  be gone in a breaking-change-friendly repository.

  '
constraints:
- In scope is runtime policy and test cleanup only.
- Do not modify historical `docs/spec/features_done/*.yaml` migration records.
- Remove `src/engineeringagent/gates.py` from active subprocess-boundary policy allowlists
  and parity policy assets.
- Replace or remove test fixtures/assertions that rely on `src/engineeringagent/gates.py`
  as an allowlisted module.
- Remove the explicit legacy-module deletion assertion test that imports `engineeringagent.gates`
  and expects `ModuleNotFoundError`.
- Preserve current loop/checks architecture boundaries and subprocess-boundary intent.
implementation_notes: "Contract change declaration\n\nSurface:\n- `architecture.loop-subprocess-boundary`\
  \ policy configuration files.\n- Fitness tests that model allowlisted-module behavior.\n\
  - Harness tests that explicitly assert legacy module import failure.\n\nOld behavior:\n\
  - Loop subprocess boundary policy still allowlists `src/engineeringagent/gates.py`.\n\
  - Fitness tests include fixtures/assertions that treat `gates.py` as a valid exempt\n\
  \  module path.\n- Harness tests include a direct import probe for `engineeringagent.gates`.\n\
  \nNew behavior:\n- Active policy allowlists include only current module paths.\n\
  - Fitness fixtures/assertions use non-legacy allowlisted modules or avoid removed-path\n\
  \  assumptions.\n- No active test explicitly imports `engineeringagent.gates` as\
  \ a legacy deletion guard.\n\nCompatibility policy:\n- Immediate breaking cleanup\
  \ of legacy references is intended.\n\nMigration plan:\n- Update subprocess-boundary\
  \ policy files to remove legacy module path entries.\n- Update fitness tests to\
  \ remove legacy allowlisted-module fixtures/assertions.\n- Remove the legacy import-deletion\
  \ test in harness suite.\n\nVerification evidence:\n- Targeted fitness and harness\
  \ tests pass after path cleanup.\n- Feature spec schema validation passes.\n\nFitness-function\
  \ impact assessment:\n- This change updates the existing `architecture.loop-subprocess-boundary`\
  \ policy\n  configuration to remove a stale allowlisted module path.\n- Path chosen:\
  \ adjust implementation/configuration to satisfy current rule intent;\n  do not\
  \ change rule ID, severity, or ownership boundaries.\n- Expected guardrail effect:\
  \ tighter allowlist accuracy, less false architectural\n  signal, and cleaner long-term\
  \ policy maintenance.\n"
acceptance:
- '`harness/fitness_functions/policies/loop_subprocess_boundary_policy.yaml` no longer
  contains `src/engineeringagent/gates.py`.'
- '`harness/fitness_functions/policies/loop_subprocess_boundary_semgrep_policy.yaml`
  no longer contains `src/engineeringagent/gates.py`.'
- Active tests do not create fixtures or assertions that treat `src/engineeringagent/gates.py`
  as an allowlisted/owned module path.
- '`tests/harness/test_gates.py` no longer contains the explicit legacy-module import
  deletion assertion.'
- '`uv run pytest -q tests/fitness/test_fitness_rules_loop_subprocess_boundary.py
  tests/fitness/test_fitness_adapters.py tests/fitness/test_fitness_rules_directionality.py
  tests/harness/test_gates.py` passes.'
- '`uv run engineeringagent validate --schema-only` passes.'
artifacts:
  plan: plan.md
updated_at: '2026-03-03T19:42:08Z'
