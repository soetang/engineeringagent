# Testing principles

- Tests follows the same structure as code under src/developer eg. test for a codex adapter belongs in tests/agents/adapters/'a relevant test filename'.py
- Tests should be self contained and may not rely on files in harness/*