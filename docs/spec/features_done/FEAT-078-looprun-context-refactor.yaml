id: FEAT-078
title: Refactor loop orchestration to pass a single LoopRun context object
type: chore
expected_commit_subject: 'chore: refactor loop to LoopRun context'
status: done
priority: medium
objective: 'Simplify the loop runtime contract by replacing long parameter lists and
  facade

  `*args/**kwargs` shims with a single pass-around `LoopRun` object that contains:

  - immutable run configuration (`RunConfig`)

  - immutable dependency/services bundle (`RunServices`)

  - copy-on-write run state (`RunState`)


  This should reduce "creative compliance" patterns (varargs shims + `__signature__`,

  ad-hoc kwarg dropping) and make orchestration code easier to read and harder to
  game,

  while preserving end-user CLI behavior.

  '
context: 'The current loop facade functions in `src/engineeringagent/loop.py` accept
  `*args/**kwargs`

  and manually bind against an `inspect.Signature` to preserve an apparent stable
  API surface.

  This pattern exists largely to avoid lint policy constraints (for example, "too
  many

  arguments") without allowing non-ignorable lint suppressions.


  We want a clearer, more robust design: a single explicit object that is passed through

  orchestration and phase functions, with explicit immutability for config/services
  and

  explicit state updates via copy-on-write.

  '
constraints:
- Use Pydantic models (no stdlib dataclasses) and set `extra="forbid"` for all new
  models.
- RunConfig and RunServices must be frozen/immutable.
- RunState must be copy-on-write (frozen + `model_copy(update=...)`), not mutated
  in place.
- 'Breaking change is allowed: update `engineeringagent.loop.run_loop` (and related
  loop entrypoints as needed) to a model-based API that accepts LoopRun.'
- Preserve CLI-visible behavior of `engineeringagent run` (flags and semantics) unless
  an explicit contract delta is documented here.
- Remove ad-hoc kwarg dropping (for example, `opencode_prompt`) from loop entrypoints;
  if an override is still needed, make it an explicit typed field.
- Add or extend a fitness function to prevent regressions back to varargs facade shims,
  `__signature__` assignments, or hidden kwarg dropping without an explicit allowlist.
- All changes must satisfy existing fitness functions and gate profiles.
implementation_notes: "Contract/API delta (explicit):\n\n- Surface: Python module\
  \ API `engineeringagent.loop.run_loop` (and related facade helpers).\n  - Old behavior:\
  \ loop entrypoints accept `*args/**kwargs` and set `__signature__` to present a\
  \ stable-looking\n    signature; they may also drop unexpected kwargs (for example,\
  \ `opencode_prompt`) before binding.\n  - New behavior: loop entrypoints accept\
  \ a single typed LoopRun object (or equivalent typed model) and do not\n    use\
  \ varargs shims or `__signature__` assignments.\n\n- Surface: tests that enforce\
  \ facade signature stability.\n  - Old behavior: `tests/test_loop_contracts.py`\
  \ asserts specific multi-parameter signatures for facade functions.\n  - New behavior:\
  \ tests assert the new LoopRun-based entrypoint contract and that config/services/state\n\
  \    immutability semantics are enforced.\n\nCompatibility policy:\n- This is an\
  \ intentional breaking change for any external Python callers relying on the old\
  \ `run_loop(...)` signature.\n- CLI behavior is intended to remain compatible: `engineeringagent\
  \ run ...` continues to accept the same flags and\n  perform the same workflow,\
  \ but will internally construct and pass a LoopRun object.\n\nFitness-function impact\
  \ assessment (required):\n- New fitness rule expected (or extension of an existing\
  \ one) to block varargs facade shims and hidden kwarg\n  dropping patterns from\
  \ reappearing.\n- Expected updates:\n  - Add `harness/fitness-functions/check_no_facade_varargs_shims.py`\
  \ (or similar) and register it in\n    `harness/fitness-functions/rules.yaml`.\n\
  \  - Regenerate the fitness rule catalog (`docs/fitness-functions/rules.md`) if\
  \ rule metadata changes.\n"
acceptance:
- '`engineeringagent run --help` output and CLI runtime behavior remain consistent
  with current documented behavior (no user-facing workflow regression).'
- '`engineeringagent.loop.run_loop` (and any other supported loop entrypoints) accept
  a single typed LoopRun object and no longer use `*args/**kwargs` + `__signature__`
  shims.'
- RunConfig and RunServices are Pydantic models with `frozen=true` and `extra=forbid`.
- RunState updates are performed via copy-on-write (`model_copy(update=...)`) and
  no in-place mutation is required for counters/maps.
- All previous ad-hoc kwarg dropping in loop entrypoints (for example, `opencode_prompt`)
  is removed; any remaining override is represented as an explicit typed field.
- A fitness function prevents reintroducing varargs facade shims / `__signature__`
  assignments / hidden kwarg dropping outside an explicit allowlist.
- '`uv run python -m engineeringagent.cli validate`, `uv run python -m engineeringagent.cli
  fitness run --format json`, and `uv run pytest -q` pass.'
subtasks:
- id: ST-001
  title: Define LoopRun, RunConfig, RunServices, RunState models (immutable + copy-on-write
    state)
  status: done
  context: 'Introduce the core run context object(s) in an appropriate loop_runtime
    module so

    dependency directionality rules remain satisfied. Ensure models are `extra=forbid`,

    RunConfig/RunServices are frozen, and RunState uses copy-on-write updates.

    '
  verification:
  - uv run python -m engineeringagent.cli validate
  - uv run pytest -q tests/test_loop_contracts.py
- id: ST-002
  title: Refactor loop controller and iteration pipeline to pass LoopRun instead of
    many scalars
  status: done
  context: 'Replace long parameter threading through orchestration functions with
    passing LoopRun.

    Keep behavior equivalent (stop conditions, selection, retries, telemetry, gate/reviewer
    sequencing).

    '
  verification:
  - uv run pytest -q tests/test_loop_runtime_iteration.py
  - uv run pytest -q tests/test_loop_opencode_integration.py
- id: ST-003
  title: Update CLI to construct LoopRun from flags and preserve run behavior
  status: done
  context: 'Keep existing CLI flags and semantics, but build RunConfig from CLI args
    and

    RunServices from the existing adapters/clients, then call the new loop entrypoint.

    '
  verification:
  - uv run python -m engineeringagent.cli run --help
  - uv run pytest -q tests/test_cli.py
- id: ST-004
  title: Update contract tests to assert the new LoopRun-based public API
  status: done
  context: 'Replace signature-stability assertions for varargs facade shims with new
    tests that:

    - assert the LoopRun entrypoint signature

    - assert immutability/copy-on-write invariants

    - assert important monkeypatch seams remain available (or are replaced with equivalent
    seams)

    '
  verification:
  - uv run pytest -q tests/test_loop_contracts.py
- id: ST-005
  title: Add fitness rule to block facade varargs shims and hidden kwarg dropping
    regressions
  status: done
  context: 'Add or extend a fitness function that scans `src/engineeringagent` and
    fails if:

    - loop entrypoints use `*args/**kwargs` facade shims

    - code assigns `__signature__ = ...` to mask varargs signatures

    - code drops unexpected kwargs to bypass binding (unless explicitly allowlisted)

    Register the rule in `harness/fitness-functions/rules.yaml` and ensure it runs
    under

    existing gate profiles.

    '
  verification:
  - uv run python -m engineeringagent.cli fitness run --format json
  - uv run pytest -q tests/test_gates.py
- id: ST-006
  title: Remove remaining facade shims from loop.py helpers
  status: done
  context: 'Continue replacing facade-style `*args/**kwargs` + `__signature__` helpers
    in `src/engineeringagent/loop.py` (notably `_run_feature_iteration` and `print_summary`)
    with explicit typed contracts while preserving monkeypatch seams and CLI-visible
    behavior.

    '
  verification:
  - uv run python -m engineeringagent.cli fitness run --format json
  - uv run pytest -q tests/test_loop_contracts.py
  notes:
  - Removed `print_summary` facade varargs/signature shim.
  - Replaced `_run_feature_iteration` facade varargs/signature shim with an explicit
    typed signature and direct `FeatureIterationInputs` construction.
- id: ST-007
  title: Archive completed feature specs before done-transition verification commands
  status: done
  context: 'Address first-run non-dry regressions where implement marks feature/subtasks
    done under docs/spec/features and then subtask verification (`engineeringagent
    validate`) fails before archive. Ensure done features are archived before running
    verification commands so done-active validation does not fail the same iteration.

    '
  verification:
  - uv run pytest -q tests/test_loop_runtime_iteration.py
  notes:
  - Reordered iteration phase execution so archive runs before verification.
  - Added regression ensuring done-transition verification observes archived state.
- id: ST-008
  title: Remove dead make_iteration_config compatibility seam from LoopRun services
  status: done
  context: 'Follow-up cleanup from reviewer feedback: remove the now-unused iteration-config
    seam from `RunServices`, simplify default service wiring, and update contract/runtime
    test stubs to match the reduced service contract.

    '
  verification:
  - uv run pytest -q tests/test_loop_contracts.py::test_loop_run_context_contract_immutability_and_extra_forbid
  - uv run pytest -q tests/test_loop_runtime_iteration.py::test_run_loop_controller_forwards_looprun_with_resolved_snapshot
  notes:
  - Removed `make_iteration_config` field from `RunServices` and default loop wiring.
  - Updated test stubs to construct `RunServices` without the removed seam.
- id: ST-009
  title: Ensure successful run archives done active specs (validate-passing)
  status: done
  context: 'Fix a regression where `engineeringagent run` could return success while
    leaving `status: done` feature YAML files under `docs/spec/features/`, causing
    `engineeringagent validate` to fail immediately afterward.'
  verification:
  - uv run pytest -q tests/test_loop_ralph_mode.py::test_run_loop_archives_done_active_feature_when_not_skip_implement
  - uv run pytest -q tests/test_loop_ralph_mode.py::test_run_loop_archives_preexisting_done_target_after_pending_completes
  notes:
  - Updated run candidate selection so done specs pending archive are processed even
    when `--skip-implement` is not set.
  - Updated integration tests to use LoopRun-based public API.
- id: ST-010
  title: README - document non-dry run side effects and validate expectations
  status: done
  context: 'Clarify that non-dry `engineeringagent run` can create a git commit and
    move completed specs from `docs/spec/features/` to `docs/spec/features_done/`.
    Explain that `engineeringagent validate` rejects `status: done` specs under `docs/spec/features/`.'
  verification:
  - uv run python -m engineeringagent.cli validate
  notes:
  - README now calls out non-dry git commit + spec archival side effects.
  - README now documents that validate rejects done specs under docs/spec/features.
- id: ST-011
  title: README - clarify uvx vs from-source invocation vs gates/spec verification
    commands
  status: done
  context: Avoid onboarding confusion by explaining that gates/spec verification commands
    are executed from the repo (e.g. `uv run ...`) even if the CLI is invoked via
    `uvx engineeringagent ...`.
  verification:
  - uv run python -m engineeringagent.cli validate
- id: ST-012
  title: Harden facade-shim fitness rule and minor readability cleanups
  status: done
  context: 'Follow-up cleanup from reviewer feedback: make back-compat unused parameters
    explicitly unused via `del`, simplify LoopRun state forwarding in the controller,
    and harden the no-facade-varargs fitness rule to detect `setattr(..., name="__signature__",
    ...)` creative-compliance patterns.'
  verification:
  - uv run python -m engineeringagent.cli fitness run --format json
  notes:
  - Added regression test to ensure signature masking via keyword `setattr` is rejected.
  - Simplified controller state forwarding and clarified unused back-compat parameter
    handling.
updated_at: '2026-02-16T07:32:38Z'
