id: FEAT-073
title: Add init packs (slim|standard) with TTY prompt and demo failing rule
type: feature
expected_commit_subject: 'feat: add init slim and standard packs'
status: done
priority: high
objective: 'Extend `engineeringagent init` with an explicit pack selection:

  - `engineeringagent init slim` (default)

  - `engineeringagent init standard` (includes a demo fitness rule that fails in pre-commit)


  When the pack is omitted and the command runs in an interactive TTY,

  prompt the user to pick a pack (default: slim). In non-interactive contexts,

  default to slim without prompting.

  '
context: "The current init scaffold is ambiguous about \"what kind of repo baseline\
  \ am I getting?\"\nand does not provide an obvious, visible example of fitness rules\
  \ failing.\n\nWe want:\n- a safe default (slim) that still runs real validations\
  \ (at least spec validation)\n- an opt-in \"standard\" pack that demonstrates the\
  \ failing-rule workflow immediately\n  by wiring an always-failing demo rule into\
  \ the pre-commit gate profile.\n"
constraints:
- 'Pack selection is a positional argument to `engineeringagent init`: `slim|standard`.'
- 'Backward-compatible default: `engineeringagent init` must remain valid.'
- If pack is omitted and stdout is a TTY, prompt for pack with default `slim`.
- If pack is omitted and stdout is not a TTY, do not prompt; default `slim`.
- If pack is explicitly provided, never prompt.
- Output must be ASCII-only.
- The final "init complete" line must include `pack=<slim|standard>`.
- Standard pack must clearly describe in terminal output that it wires a demo fitness
  function that always fails into pre-commit.
implementation_notes: "Contract/API delta (explicit):\n\n- Surface: `engineeringagent\
  \ init` CLI.\n  - Old behavior: `engineeringagent init` has no pack concept.\n \
  \ - New behavior: `engineeringagent init [PACK]` where PACK is `slim` or `standard`.\n\
  \nCompatibility policy:\n- `engineeringagent init` with no pack remains valid and\
  \ defaults to `slim`.\n- In TTY usage, omission triggers a prompt; in non-TTY usage,\
  \ omission does not.\n\nImplementation approach notes:\n- Use a deterministic prompt\
  \ with fixed options and default slim.\n- Keep `--scaffold-profile` orthogonal;\
  \ it remains the \"toolchain profile\" axis.\n"
acceptance:
- '`engineeringagent init --help` documents the optional positional pack argument.'
- When pack is omitted and TTY is detected, init prompts to select slim|standard (default
  slim).
- When pack is omitted and TTY is not detected, init does not prompt and uses slim.
- When pack is provided (`init slim` or `init standard`), init never prompts.
- Init completion output includes `pack=...`.
- Standard pack prints a one-line explanation that it installs a demo failing fitness
  rule into pre-commit.
- The standard-pack demo failing rule includes a remediation that explains how to
  disable the demo failure (by switching to slim and/or removing the demo gate/manifest).
subtasks:
- id: ST-001
  title: Add pack arg and TTY-only prompt with deterministic default
  status: done
  context: 'Extend the Typer `init` command to accept `slim|standard` as an optional
    positional.

    Implement TTY detection and prompt only when pack is omitted and TTY is true.

    Add unit tests covering TTY vs non-TTY behavior and "no prompt when pack provided".

    '
  verification:
  - uv run pytest -q tests/test_init_command.py
- id: ST-002
  title: Wire standard pack demo-fail into precommit gate plan
  status: done
  context: 'Add standard-pack scaffold differences that cause pre-commit to run a
    demo

    fitness rule that always fails (without making the default `fitness run` unusable).

    Add tests verifying slim vs standard outputs differ as intended.

    '
  verification:
  - uv run pytest -q tests/test_init_command.py
- id: ST-003
  title: Stop --skip-implement from looping when gates pass
  status: done
  context: '`engineeringagent run --skip-implement` is documented as gates-only mode,
    but it can loop/retry the same passing iteration until hitting --max-iterations.


    Ensure gates-only runs execute a single iteration and exit (0 on pass, 1 on fail).'
  verification:
  - uv run pytest -q tests/test_loop_ralph_mode.py -k skip_implement_exits_after_one_passing_iteration
- id: ST-004
  title: README quickstart coherence for init + run (dirty worktree, mutations)
  status: done
  context: 'Update README.md to cover: clean-worktree requirement (commit or --allow-dirty)
    after init/spec edits, that non-dry run mutates feature YAML + writes progress
    logs, and document `engineeringagent init [slim|standard]` pack behavior (including
    TTY prompt + how to avoid it).'
  verification:
  - uv run pytest -q tests/test_repo_readme_process_reviewer_activation.py
- id: ST-005
  title: Align uv-llms docs with --skip-implement behavior
  status: done
  context: Update docs/references/uv-workflow.md examples and guidance to match gates-only
    runtime behavior (no silent looping/retries when gates pass).
  verification:
  - uv run pytest -q tests/test_reviewer_reference_docs.py
- id: ST-006
  title: Improve run diagnostics when default OpenCode implement cannot proceed
  status: done
  context: Non-dry `engineeringagent run` can appear to hang when default OpenCode
    implement mode cannot run. Add clear terminal output pointing to --implement-command/--skip-implement
    and where logs are written.
  verification:
  - uv run pytest -q tests/test_loop_opencode_integration.py
- id: ST-007
  title: Fix progress log path locality violation in OpenCode precheck hint
  status: done
  context: Remove progress artifact path literals from loop runtime diagnostics by
    constructing progress references via engineeringagent.progress_paths.
  verification:
  - uv run python -m engineeringagent.cli fitness run --format json
- id: ST-008
  title: Deduplicate spec validation gate scaffold
  status: done
  context: Refactor init scaffold gate construction so the spec_validate gate shape
    comes from a single helper, reducing drift risk between slim and standard packs.
  verification:
  - uv run pytest -q tests/test_init_command.py
- id: ST-009
  title: Format README for mdformat gate
  status: done
  context: 'The precommit gate profile includes mdformat validation for README.md.

    Ensure README.md is mdformat-compliant so `engineeringagent-precommit` can pass.

    '
  notes:
  - 'Fixes reported failure: "Error: File README.md is not formatted."'
  verification:
  - uv run mdformat --check README.md AGENTS.md docs/references/docs-architecture.md
updated_at: '2026-02-15T21:53:41Z'
