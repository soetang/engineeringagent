id: FEAT-080
title: Restructure tests into topic folders and have ruff_validate lint harness/
type: test
expected_commit_subject: "test: restructure tests and lint harness"
status: backlog
priority: medium
objective: |
  Make the test suite easier to navigate by grouping test modules into
  topic-oriented folders under `tests/`, while keeping pytest discovery,
  fixtures, and gates behavior stable.

  Additionally, align the `ruff_validate` gate with its `on_change` triggers by
  ensuring the gate lints `harness/` code (without unintentionally forcing the
  full docstring/complexity policy onto harness fitness-function scripts).
context: |
  The repository currently has many flat test modules under `tests/`, which is
  hard to scan and discourages adding more tests.

  The current gate config already triggers `ruff_validate` on changes under
  `harness/**/*.py`, but the configured command only runs Ruff on
  `src/engineeringagent`. This is inconsistent and means harness changes can
  bypass lint checks.
constraints:
  - Keep `tests/conftest.py` and `tests/fixtures/` at `tests/` root (no move).
  - Keep test topic folders only one level deep (e.g. `tests/cli/test_*.py`).
  - After the move, update any tests that compute repo root via
    `Path(__file__).resolve().parents[1]` so they still resolve the repo root
    correctly.
  - Preserve pytest collection via `uv run pytest -q` without adding a new
    `pytest.ini` file.
  - Update this repo's `harness/gates.yaml` so `ruff_validate` lints
    `src/engineeringagent` and `harness/`.
  - Update `DEFAULT_GATE_CONFIG` in `src/engineeringagent/gates.py` so
    scaffolded/missing-gates behavior matches the repository gate policy.
  - Ensure `uv run ruff check harness` passes by using Ruff configuration
    (preferred) rather than scattering `noqa` suppressions.
  - Record the exact move map in this spec and execute it deterministically.
implementation_notes: |
  Concrete move map (no renames besides directory move):

  - tests/test_cli.py -> tests/cli/test_cli.py
  - tests/test_cli_reviewers.py -> tests/cli/test_cli_reviewers.py
  - tests/test_cli_typer_parity_helpers.py -> tests/cli/test_cli_typer_parity_helpers.py
  - tests/test_init_command.py -> tests/cli/test_init_command.py
  - tests/test_init_scaffold.py -> tests/cli/test_init_scaffold.py

  - tests/test_loop_contracts.py -> tests/loop/test_loop_contracts.py
  - tests/test_loop_opencode_integration.py -> tests/loop/test_loop_opencode_integration.py
  - tests/test_loop_output.py -> tests/loop/test_loop_output.py
  - tests/test_loop_ralph_mode.py -> tests/loop/test_loop_ralph_mode.py
  - tests/test_loop_reviewers.py -> tests/loop/test_loop_reviewers.py
  - tests/test_loop_runtime_iteration.py -> tests/loop/test_loop_runtime_iteration.py
  - tests/test_loop_runtime_time_format.py -> tests/loop/test_loop_runtime_time_format.py
  - tests/test_loop_selection.py -> tests/loop/test_loop_selection.py

  - tests/test_fitness_adapters.py -> tests/fitness/test_fitness_adapters.py
  - tests/test_fitness_catalog_generation.py -> tests/fitness/test_fitness_catalog_generation.py
  - tests/test_fitness_contract.py -> tests/fitness/test_fitness_contract.py
  - tests/test_fitness_manifest.py -> tests/fitness/test_fitness_manifest.py
  - tests/test_fitness_manifest_contract.py -> tests/fitness/test_fitness_manifest_contract.py
  - tests/test_fitness_parallel_runner.py -> tests/fitness/test_fitness_parallel_runner.py
  - tests/test_fitness_registry.py -> tests/fitness/test_fitness_registry.py
  - tests/test_fitness_rule_id_collisions.py -> tests/fitness/test_fitness_rule_id_collisions.py
  - tests/test_fitness_rules_directionality.py -> tests/fitness/test_fitness_rules_directionality.py
  - tests/test_fitness_rules_harness_root_yaml_only.py -> tests/fitness/test_fitness_rules_harness_root_yaml_only.py
  - tests/test_fitness_rules_logging_path_locality.py -> tests/fitness/test_fitness_rules_logging_path_locality.py
  - tests/test_fitness_rules_loop_subprocess_boundary.py -> tests/fitness/test_fitness_rules_loop_subprocess_boundary.py
  - tests/test_fitness_rules_markdown_locality.py -> tests/fitness/test_fitness_rules_markdown_locality.py
  - tests/test_fitness_rules_markdown_references.py -> tests/fitness/test_fitness_rules_markdown_references.py
  - tests/test_fitness_rules_prompt_locality.py -> tests/fitness/test_fitness_rules_prompt_locality.py
  - tests/test_fitness_rules_scaffold_template_locality.py -> tests/fitness/test_fitness_rules_scaffold_template_locality.py
  - tests/test_fitness_rules_source_first_loop_commands.py -> tests/fitness/test_fitness_rules_source_first_loop_commands.py
  - tests/test_fitness_side_effect_contract.py -> tests/fitness/test_fitness_side_effect_contract.py

  - tests/test_gates.py -> tests/harness/test_gates.py
  - tests/test_harness_script_relocation.py -> tests/harness/test_harness_script_relocation.py

  - tests/test_opencode_client.py -> tests/opencode/test_opencode_client.py
  - tests/test_opencode_permissions.py -> tests/opencode/test_opencode_permissions.py

  - tests/test_docs_reviewer_agents_reference.py -> tests/reviewers/test_docs_reviewer_agents_reference.py
  - tests/test_repo_readme_process_reviewer_activation.py -> tests/reviewers/test_repo_readme_process_reviewer_activation.py
  - tests/test_reviewer_reference_docs.py -> tests/reviewers/test_reviewer_reference_docs.py
  - tests/test_reviewers_contract.py -> tests/reviewers/test_reviewers_contract.py
  - tests/test_reviewers_parse.py -> tests/reviewers/test_reviewers_parse.py
  - tests/test_reviewers_runtime.py -> tests/reviewers/test_reviewers_runtime.py
  - tests/test_reviewers_sandbox.py -> tests/reviewers/test_reviewers_sandbox.py
  - tests/test_reviewers_state.py -> tests/reviewers/test_reviewers_state.py

  - tests/test_git_client.py -> tests/vcs/test_git_client.py
  - tests/test_commit_message_validation.py -> tests/vcs/test_commit_message_validation.py

  - tests/test_validator.py -> tests/meta/test_validator.py
  - tests/test_coverage_threshold_regressions.py -> tests/meta/test_coverage_threshold_regressions.py

  Gate behavior delta (explicit):

  - Surface: `ruff_validate` default gate runner.
    - Old behavior: `ruff_validate` runs `uv run ruff check src/engineeringagent`.
    - New behavior: `ruff_validate` runs `uv run ruff check src/engineeringagent harness`.

  Compatibility policy:
  - This is a tightening of validation for repositories that adopt the default
    gate config. To avoid unexpected failures from harness fitness-function
    scripts, configure Ruff to ignore docstring/complexity policy for
    `harness/fitness-functions/*.py` while still linting for baseline errors.

  Fitness-function impact assessment:
  - no fitness-function changes required; this change is a test layout and lint
    gate/config alignment only.
acceptance:
  - "All existing tests are relocated under topic folders as defined in the move map, without renaming test modules."
  - "`tests/conftest.py` and `tests/fixtures/` remain at `tests/` root and continue to work."
  - "Any repo-root path resolution in tests remains correct after relocation (no tests rely on `parents[1]` meaning repo root)."
  - "`uv run pytest -q` passes."
  - "`ruff_validate` gate command lints `harness/` as well as `src/engineeringagent`."
  - "`uv run ruff check src/engineeringagent harness` passes in this repo."
  - "Docs that describe the canonical Ruff command are updated to mention the harness path."
subtasks:
  - id: ST-001
    title: Move tests into topic folders under tests/
    status: backlog
    context: |
      Create topic folders and move each test module according to the move map
      in this spec. Do not move `tests/conftest.py` or `tests/fixtures/`.
    verification:
      - uv run pytest -q
  - id: ST-002
    title: Fix tests that compute repo root via Path(__file__).parents
    status: backlog
    context: |
      Update tests that use `Path(__file__).resolve().parents[1]` so they still
      resolve the repository root after being moved one directory deeper.
      Prefer a robust approach that remains correct if additional nesting is
      introduced later (e.g. pytest rootpath fixture or a centralized helper).
    verification:
      - uv run pytest -q
  - id: ST-003
    title: Update ruff_validate gate to lint harness/
    status: backlog
    context: |
      Align gate behavior with `on_change` by updating both this repo's
      `harness/gates.yaml` and `src/engineeringagent/gates.py` default config so
      `ruff_validate` runs Ruff on `src/engineeringagent` and `harness/`.
      Update any tests that assert the exact gate runner string.
    verification:
      - uv run pytest -q tests/harness/test_gates.py
      - uv run python -m engineeringagent.cli gates run --profile precommit
  - id: ST-004
    title: Configure Ruff so harness fitness-function scripts pass lint
    status: backlog
    context: |
      `uv run ruff check harness` currently fails due to docstring and
      complexity rules applied to harness scripts. Update `pyproject.toml`
      Ruff settings to ignore these rules for `harness/fitness-functions/*.py`
      (and only for that path), while keeping baseline linting enabled.
    verification:
      - uv run ruff check src/engineeringagent harness
  - id: ST-005
    title: Update docs referencing Ruff command and run full regression
    status: backlog
    context: |
      Update references in docs that currently state `uv run ruff check
      src/engineeringagent` to match the new canonical lint command.
      Run the full precommit profile gates and the full pytest suite.
    verification:
      - uv run python -m engineeringagent.cli validate
      - uv run ruff check src/engineeringagent harness
      - uv run pyright src/engineeringagent tests harness
      - uv run pytest -q
      - uv run python -m engineeringagent.cli gates run --profile precommit
updated_at: "2026-02-15T00:00:00Z"
