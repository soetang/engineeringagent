id: FEAT-167
title: Feed failed command output into retry prompts
type: feature
expected_commit_subject: 'feat: feed failed command output into retry prompts'
status: backlog
priority: high
objective: Reduce retry reruns by forwarding failed command output context into the
  next implement prompt for command failures.
context: |
  Today, retries often include command identity but not enough failure output context,
  so the model reruns failing commands to recover diagnostics. Over long conversations,
  this increases token cost.

  This feature is specifically about feed-in context. Command output optimization
  itself is deferred.
constraints:
  - Forward failed command output context into retry feedback for command check failures
    in checks/gates command paths and for verification command failures.
  - 'Include at minimum: exact command, return code, and a failure output excerpt.'
  - Forward latest-only failure context for retry prompts (no accumulating history).
  - Keep full raw command outputs in progress logs and telemetry for human debugging.
  - Preserve loop/checks ownership boundaries for prompt feedback shaping.
  - Preserve prompt renderer no-truncation policy (no truncation-by-slicing in renderer).
  - 'Out of scope: command output minimization/tuning (for example pytest coverage table
    formatting or global command verbosity changes).'
implementation_notes: |
  Contract change declaration

  Surface:
  - checks command-failure `prompt_feedback` content
  - verification failure feedback content forwarded into the next implement prompt

  Old behavior:
  - Retry context often lacks concrete failing output lines; the model reruns commands
    to inspect diagnostics.

  New behavior:
  - Retry context includes bounded failed-command output excerpts plus command metadata.

  Compatibility policy:
  - Feedback enrichment only; no command execution flow changes.

  Fitness-function impact assessment
  - no fitness-function changes required.
acceptance:
  - On command check failure, next retry feedback includes failed command output excerpt.
  - On verification command failure, next retry feedback includes failed command output
    excerpt.
  - Retry feedback includes exact command and return code with the excerpt.
  - On repeated failures, only the latest failure context is forwarded.
  - Existing progress logs retain full outputs for debugging.
  - Existing feedback boundary and no-truncation rules remain satisfied.
  - The feature spec validates with `uv run engineeringagent validate --schema-only`.
subtasks:
  - id: ST-001
    title: Enrich checks command-failure prompt feedback with output excerpts
    status: backlog
    context: |
      Extend checks command-failure feedback to include recent failed-command output
      excerpts that are actionable for retries.
    verification:
      - uv run pytest -q tests/checks/test_run_checks_contract.py -k prompt_feedback
  - id: ST-002
    title: Enrich verification command-failure feedback with output excerpts
    status: backlog
    context: |
      Include failed-command output excerpts in verification retry feedback for the
      next implement pass.
    verification:
      - uv run pytest -q tests/loop/test_loop_phases_coverage.py -k verification
      - uv run pytest -q tests/loop/test_feedback_contracts.py -k command_failure
  - id: ST-003
    title: Add focused regression tests for latest-only retry context
    status: backlog
    context: |
      Verify repeated failures replace prior retry context and forward only the latest
      failure excerpt.
    verification:
      - uv run pytest -q tests/loop/test_feedback_contracts.py
      - uv run pytest -q tests/loop/test_loop_phases_coverage.py
  - id: ST-004
    title: Run schema validation and targeted iteration-end checks
    status: backlog
    context: |
      Confirm contracts and loop/checks boundaries remain valid after feedback enrichment.
    verification:
      - uv run engineeringagent validate --schema-only
      - uv run engineeringagent checks run --phase iteration_end
updated_at: '2026-03-03T00:00:00Z'
