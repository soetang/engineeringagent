id: FEAT-126
title: Extract shared checks planner helper for phase and on_change decisions
type: chore
expected_commit_subject: 'chore: add FEAT-126 shared checks planning policy'
status: backlog
priority: high
objective: 'Centralize duplicated checks planning logic (phase defaults, manual behavior,
  on_change matching, and fallback reasons) into a shared planner/helper module used
  by commands, fitness, and reviewers runtimes.

  '
context: 'Current checks planners in `commands/runtime.py`, `fitness/runtime.py`, and
  `reviewers/runtime.py` re-implement the same policy decisions with near-identical
  code.

  This duplication increases drift risk and makes incremental policy changes harder
  to apply consistently.

  We want one shared planning helper for decision policy only, with type-specific
  adapters so behavior remains deterministic while ownership and maintenance improve.

  This feature is explicitly not about coupling execution/subprocess behavior across
  check types.

  '
constraints:
- Preserve existing run/skip outcomes and reason strings for all three planners.
- Scope is planner/helper extraction only; do not merge or centralize command/fitness/reviewer
  execution implementations.
- Preserve existing manual-phase behavior (manual plans remain skip-only).
- Keep `engineeringagent.checks.run_checks(...)` external behavior unchanged.
- Keep check-specific runtime module public helpers available unless a direct replacement
  is introduced in the same change.
- Keep deterministic ordering of planned entries and output text unchanged.
- Keep compatibility with existing changed-path fallback behavior and reason propagation.
- Shared planner/helper module must remain execution-agnostic (no imports from command
  execution, fitness adapters, or reviewer engine execution surfaces).
- Command execution stays in `checks/commands/runtime.py`; fitness execution stays in
  `checks/fitness/runtime.py`; reviewer execution stays in `checks/reviewers/runtime.py`
  and `checks/reviewers/engine.py`.
- 'no fitness-function changes required; this refactor consolidates existing policy
  logic without changing architectural boundaries or check execution semantics.'
implementation_notes: |
  Contract change declaration (internal-only)

  Surface:
  - Internal checks planner/helper functions used by command, fitness, and reviewer runtimes.
  - Internal phase/default resolution helpers currently duplicated per runtime module.

  Old behavior:
  - Each runtime module implements local copies of default/effective phase calculation
    and on_change run/skip policy.
  - Reason constants and manual/on_change branches are duplicated across modules.

  New behavior:
  - A shared planner/helper module owns default phase resolution and run/skip decision
    logic.
  - Runtime modules keep type-specific filtering/execution but delegate policy decisions
    to shared helpers.
  - Execution remains per-runtime and is intentionally not centralized.
  - Existing reason values, ordering, and planner outputs remain behaviorally equivalent.

  Compatibility policy:
  - No user-facing CLI/API contract changes are intended.
  - Internal helper signatures may change as part of consolidation, but runtime module
    behavior must remain stable.

  Migration plan:
  - Introduce shared planner/helper policy functions under `src/engineeringagent/checks/`.
  - Migrate command planner first, then fitness planner, then reviewer planner.
  - Add/adjust parity tests to lock decision equivalence before removing duplicate helpers.

  Verification evidence:
  - Targeted checks planner/runtime tests for commands, fitness, and reviewers.
  - checks API contract tests and repository validation.
acceptance:
- Shared planner/helper functions exist and are used by command, fitness, and reviewer
  runtime planners.
- Shared planner/helper module is execution-agnostic and does not own subprocess/agent
  execution behavior.
- Duplicated `_effective_default_phase(...)` and equivalent on_change policy branches
  are removed from per-type runtime modules.
- Existing reason labels remain unchanged (`always_run_no_on_change`, `matched_on_change`,
  `no_on_change_match`, manual/fallback reasons).
- Planner outputs remain deterministic and behaviorally equivalent across unchanged
  test fixtures.
- '`uv run pytest -q tests/checks/test_commands_runtime.py tests/checks/test_fitness_runtime.py
  tests/checks/test_checks_reviewers_runtime.py tests/checks/test_run_checks_contract.py`
  passes.'
- '`uv run engineeringagent validate` passes.'
subtasks:
- id: ST-001
  title: Add shared checks planning policy module
  status: backlog
  context: Introduce reusable helpers for effective phase resolution and on_change
    run/skip decisions with deterministic reason output.
  verification:
  - uv run pytest -q tests/checks/test_commands_runtime.py tests/checks/test_fitness_runtime.py
- id: ST-002
  title: Migrate command planner to shared policy
  status: backlog
  context: Refactor command planner to delegate policy decisions while preserving
    output and invocation behavior.
  verification:
  - uv run pytest -q tests/checks/test_commands_runtime.py tests/checks/test_commands_group_port.py
- id: ST-003
  title: Migrate fitness planner to shared policy
  status: backlog
  context: Refactor fitness planner to use shared policy without changing selection
    behavior or failure payload semantics.
  verification:
  - uv run pytest -q tests/checks/test_fitness_runtime.py tests/checks/test_fitness_group_port.py
- id: ST-004
  title: Migrate reviewer planner to shared policy
  status: backlog
  context: Refactor reviewer runtime planner to use shared policy and keep reviewer
    state/cache execution semantics unchanged.
  verification:
  - uv run pytest -q tests/checks/test_checks_reviewers_runtime.py tests/reviewers/test_reviewers_runtime.py
- id: ST-005
  title: Add parity regressions and remove duplicate planner helpers
  status: backlog
  context: Lock decision equivalence with focused parity tests, then remove obsolete
    duplicated helper code paths.
  verification:
  - uv run pytest -q tests/checks/test_commands_runtime.py tests/checks/test_fitness_runtime.py
    tests/checks/test_checks_reviewers_runtime.py
- id: ST-006
  title: Run integration verification for checks surface
  status: backlog
  context: Confirm consolidated planner policy does not change checks API outcomes
    in combined group runs.
  verification:
  - uv run pytest -q tests/checks/test_run_checks_contract.py tests/harness/test_checks_runtime.py
  - uv run engineeringagent validate
updated_at: '2026-02-24T12:10:00Z'
